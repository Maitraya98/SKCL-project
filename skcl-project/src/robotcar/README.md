# RobotCar pipeline — not recovered

The scripts that should live here (`road_annotations.py`, `extract_road_dataset.py`,
`demosaic_robotcar.py`, `robotcar_lt.py`, `robotcar_multiview.py`,
`robotcar_hierarchy.py`, `survey_road_dataset.py`, `analyze_box_sizes.py`) were
referenced by name and description in past chats but their code was never quoted
in full, so it couldn't be reconstructed. See the root `NOTES.md` for details.
